# AGE动漫自动追更下载器 · 使用说明

> 番仓 AniVault（0.9.0）：桌面面板可读/改/存 `content.json`，并可在面板触发 dry-run/下载、停止、查看实时日志与待下载清单；下载引擎可在 aria2/idm/ffmpeg 间切换（默认 aria2）；以下内容是自动更新脚本与桌面面板的联合使用说明。

0.9.0 行为说明：Edge 浏览器实例复用并为并发取址创建独立 context；`PROGRESS.md`/`BLOCKED.md` 超过 2000 行时自动轮转；面板日志仅读取文件尾部；站点地址按文件缓存；连接和 aria2 参数保持现有兼容行为。

0.8.2 行为说明：界面再标准化——全局设置改为左标签（约 120px）右控件垂直单列，两个复选框单独一行；所有 input/select 统一样式（高 30px、圆角 6px、边框 #BCCCD9、内边距 6px 10px），数字类约 160px 定宽，长文本弹性拉伸，hint 一律放控件下方；列表行 = 行首箭头 + 名称输入 + 开/关（0.8.0 勾选框+即时文字）+「编辑」（切换展开）+「删除」（危险色 #B3261E），条目 hover 浅底色 #F0F4F8，空状态显示“暂无追番，点击右上角添加番剧”；窗口 ≤1000px 自动单列堆叠（全局设置→追番清单→运行控制，运行两栏变单栏）；区块标题 16px/700、表单标签 13px/400、hint 11px #829AB1，模块间距 16px、表单项 12px、标签与输入 8px；卡片头部移除“片名”文字标签；新增 `tests/ui-check.js` 自动检查这些规范。其余沿用：日志与清单固定 320px 各自滚动；启停控件勾选=开/取消=关整块可点；清单表格有“已下载/总数”与行“删除”；站点地址面板可改（base_url）；时间入口只有 fetch_time，保存后同步 AniVaultAutoRun；每部番可单独停用；每集状态实时显示；部分成功推进进度；attempt_timeout_min 控制单次等待上限。

## 一、项目简介

本项目是一套本地自动追更工具，每天 18 点由 Windows 计划任务自动运行，读取 `content.json` 中的追番清单，到 AGE动漫（agedm.io）检查更新。有新集时，程序用无头 Edge 抓取视频直链，下载到 `D:\idm下载` 对应文件夹，完成后自动更新进度并同步文件夹名。

运行时不需要 AI，也不消耗 token，只依赖本机 Node、Edge、IDM 和 ffmpeg。

## 二、文件说明

## 下载通道

默认优先线路仍然是西瓜线路 1。0.4.0 起下载引擎可在 `defaults.download_engine` 选择：
aria2（默认）/ idm / ffmpeg，面板下拉即可切换。MP4 所选引擎优先，失败按 aria2→ffmpeg→idm
兜底；M3U8 只用 ffmpeg/aria2，永不走 IDM。IDM 仅在面板交互模式（AGE_RUN_MODE=interactive）
下生效；`auto_close_idm` 仅当本次有 IDM 成功下载时才关闭 IDM。并发由
`defaults.max_parallel` 控制，默认 1、范围 1-10、推荐 3。网络请求带 `Connection: close`，
30 秒超时，fetch 失败自动重试 1 次（间隔 3 秒）；单次下载等待上限由
`defaults.attempt_timeout_min` 控制（默认 20、范围 5-60）。站点地址可用 `AGE_BASE` 覆盖（默认
https://www.agedm.io）。网络失败不再中断整个运行：首页失败记 BLOCKED 后继续，单部番失败
记 BLOCKED 后继续下一部。DNS 可尝试 119.29.29.29（腾讯）或 223.5.5.5（阿里）。

| 文件 | 作用 |
| --- | --- |
| `anime_updater.js` | 主程序，负责读配置、查更新、取直链、调 IDM、写回进度、改名、同步计划任务 |
| `content.json` | 追番配置与进度记录，是唯一事实来源 |
| `task_state.json` | 记录计划任务上次同步的抓取时间和运行模式（interactive / password） |
| `PROGRESS.md` | 运行日志，每次运行追加 |
| `BLOCKED.md` | 异常/待裁决记录（搜不到片名、下载失败、配置冲突等） |
| `local/` | 本地辅助文件目录，保存错误报告、GitHub 教程和自动生成的待下载清单，整个目录不进入 Git |
| `local/待下载清单.csv` | 每次运行生成的缺失集清单 |
| `VERSION` | 当前版本号 |
| `.gitignore` | Git 忽略规则，忽略 `local/` 和 ffmpeg 二进制文件 |
| `.git` | git 版本库，回滚点见文末版本记录 |

## 三、content.json 配置说明

### 3.1 顶层结构

```json
{
  "fetch_time": "18:00",
  "defaults": {
    "max_download": 10,
    "auto_repair": true,
    "auto_close_idm": true
  },
  "anime": {
    "片名": { "site_id": 12345678 }
  }
}
```

### 3.2 defaults（全局默认，对所有动漫生效）

| 字段 | 默认值 | 含义 |
| --- | --- | --- |
| `max_download` | 10 | 每次运行最多下载多少集（防止老番一次全量下载） |
| `auto_repair` | true | 发现已记录集数缺失时自动补下，而不是一直 BLOCKED |
| `auto_close_idm` | true | 全部下载完成后，若 IDM 空闲 60 秒则自动关闭 |

### 3.3 每部动漫字段

| 字段 | 必填 | 说明 |
| --- | --- | --- |
| `site_id` | 可空 | 站内详情页 id，为空时脚本自动搜索并补全 |
| `update_time` | 可空 | 本周放送时段（如 22 点），仅作参考，脚本会自动补或留空 |
| `downloaded_start` | 可空 | 已下载的起始集数（老格式文件夹显示用），新条目默认 0 |
| `downloaded_end` | 可空 | **已下载到第几集，做差下载的权威依据**；为空时回退解析文件夹名 |
| `site_latest` | 可空 | 站内西瓜线路最新集数，每次运行自动更新 |
| `folder_name` | 可空 | 文件夹名模板，不填则用老格式 `前缀_片名起-止` |
| `file_name` | 可空 | 视频文件名模板，不填则用 `片名 第NN集 - 在线播放 - AGE动漫.mp4` |

### 3.4 模板占位符

| 占位符 | 含义 | 示例 |
| --- | --- | --- |
| `{name}` | 片名 | `尼古喵喵` |
| `{ep}` | 集数（两位补零） | `06` |
| `{end}` | 下载完成后的最新集数 | `6` |
| `{start}` | 起始集数 | `1` |

例如 `"folder_name": "{name}_{end}"` 生成 `尼古喵喵_6`；`"file_name": "{name}_{ep}.mp4"` 生成 `尼古喵喵_06.mp4`。

### 3.5 只写片名会自动补全吗

会。只写 `"片名": {}` 时，脚本自动补 `site_id`（站内搜索）、`update_time`（放送列表，搜不到为 null）、`downloaded_start/end`（有同名文件夹则回填，否则 0/0）、`site_latest`（运行时更新）。注意 `downloaded_end=0` 时本次最多下载 `max_download` 集（默认 10）。

## 四、日常操作

### 4.1 添加/修改动漫

编辑 `content.json`，新条目按下面的格式写。

```json
"新动漫名字": {
  "downloaded_end": 5,
  "folder_name": "{name}_{end}",
  "file_name": "{name}_{ep}.mp4"
}
```

改完建议先跑 dry-run 验证格式和匹配。

### 4.2 修改抓取时间

把顶层 `"fetch_time"` 改成其他时间（如 `"20:00"`）。下次任意一次运行会自动同步计划任务；如果任务处于密码模式（`run_mode: password`），需要带账号密码环境变量重新 `--install-task` 才能自动改时间（见 4.4）。

### 4.3 运行命令

```powershell
# 只查更新、列清单，不下载不改名不建文件夹
node D:\project_codex\AGE动画_GET\anime_updater.js --dry-run

# 立即执行完整下载
node D:\project_codex\AGE动画_GET\anime_updater.js

# 创建/更新计划任务（读取 fetch_time）
node D:\project_codex\AGE动画_GET\anime_updater.js --install-task
```

### 4.4 计划任务

- 任务名叫 `AGEAnimeUpdater`，每天按 `fetch_time` 指定的时间运行。
- 已切换为“登录/非登录都运行”（Logon Mode Interactive/Background），使用微软账户在线密码配置。
- 密码模式下，改 `fetch_time` 需要手动带凭据重建任务。

```powershell
$env:AGE_TASK_USER='HEATH\15269'
$env:AGE_TASK_PASS='你的微软账户密码'
node D:\project_codex\AGE动画_GET\anime_updater.js --install-task
Remove-Item Env:AGE_TASK_USER,Env:AGE_TASK_PASS
```

### 4.5 查看结果

- 进度看 `PROGRESS.md`
- 异常看 `BLOCKED.md`
- 缺失集看 `local/待下载清单.csv`

## 五、注意事项（坑）

1. **JSON 必须严格**。英文逗号、英文引号、不能有尾逗号；改完先 `--dry-run`。
2. **`downloaded_end` 语义**。是“已下到第几集”，写小会重复下载，写大会漏下；`downloaded_start` 只影响文件夹显示。
3. **站内搜索模糊匹配**。找不到完全一致的片名会用第一个相似结果，新条目跑完 dry-run 后核对 `site_id`。
4. **`update_time` 不自动刷新**。只在为空时补；番剧改播出时段需手动清空该项。
5. **计划任务登录模式**。已配置为登录/非登录都运行；改时间见 4.4，避免任务被悄悄降级回“仅登录运行”。
6. **更新时段晚于 18 点** 的番，当天 18 点可能抓不到，次日补。
7. **新番会分批下载**。受 `max_download` 限制，每次最多 N 集，跑多次才能追平。
8. **磁盘空间**。每集约 400 到 700MB，连续追多部注意剩余空间。
9. **直链有时效**。脚本每次现抓现用，不要手动保存旧直链。
10. **取址偶发失败**。播放页偶尔拿不到直链，脚本会重试一次并换线路兜底；连续失败进 BLOCKED。
11. **完成判定**。文件 ≥100MB 且 120 秒大小不变才算完成。ffmpeg 下载时先写 `.part.mp4`，成功后再改成正式文件名，中途中断的文件不会计入集数。
12. **文件命名别乱改**。脚本靠文件名识别集数（老格式和 `file_name` 模板格式都认），改成别的格式会被当成缺失。
13. **手动删文件**。`auto_repair: true` 时会自动补下缺失集；不需要自动补可改为 false。
14. **IDM 弹窗**。交互模式的 MP4 直链仍会以最小化方式启动 IDM，下载完自动关闭。密码后台任务使用 ffmpeg，不依赖桌面中的 IDM。
15. **不要乱改 `task_state.json`**。`run_mode` 影响计划任务同步策略，改错可能导致任务被重建为仅登录运行。
16. **git 回滚注意**。`git reset --hard v1.x` 会丢弃未提交的改动（含你手动改的 content.json），回滚前先提交或备份。

## 六、版本记录

| 版本 | 解决的问题 / 新增内容 |
| --- | --- |
| v0.8.2 | 表单与列表标准化：全局设置左标签右控件垂直单列、input/select 统一样式（30px/圆角6px/#BCCCD9/6px 10px）、数字定宽、hint 置控件下方；列表行=箭头+名称输入+开/关+「编辑」+「删除」（危险色），hover 浅底色、空状态文案；≤1000px 响应式单列；字体层级与留白统一（标题16px/700、标签13px/400、hint 11px）；去掉卡片头部“片名”标签；新增 tests/ui-check.js 防回归。 |
| v0.9.0 | 性能优化：无头 Edge 浏览器一次运行只启动一个实例（并发取址各自独立 context）；PROGRESS/BLOCKED 超 2000 行自动轮转（保留尾部与轮转标记）；面板日志只读文件末尾约 64KB；getBase 站点地址按文件缓存；连接头 Connection: close 与 aria2 -x16 保持现状（防 fetch failed 与限流，后续再调）。 |
| v0.8.1 | 修复 0.8.0 引入的日志滚动回归：.log 与 .csv-scroll 固定 height:320px（保留 overflow:auto、min-height:0），.run-columns > div 用 flex 列布局保证两栏等高，内容超出各自内部滚动、页面不再被撑高。 |
| v0.8.0 | 界面排版升级：启停控件统一为“勾选框+开/关”（整块可点、文字即时切换）；全局设置两列、卡片头部对齐、控件/按钮统一 30px 与主次层级、日志与清单等高各自滚动、表格操作列固定宽、:focus-visible 焦点可见、窗口缩放不破版；头部去掉 Demo 并加版本徽标；新增 --screenshot 截图自检模式（package.json screenshot 脚本）。 |
| v0.7.0 | 待下载清单增强：表格新增“已下载/总数”汇总列（total=site_latest 或 downloaded_end，文件夹不可读显示 ?）与行“删除”（写入 skip_eps，之后不再列出，auto_repair 不补回，卡片可查看/清空）；站点地址面板可改（base_url 入 content.json，优先级 AGE_BASE > base_url > 默认）；卡片头部只读显示“是否启用: 对/错”。 |
| v0.6.1 | 合并重复时间入口：删除 0.6.0 引入的第二个时间字段，计划任务 AniVaultAutoRun 统一由 fetch_time 驱动（填 HH:MM 创建/更新、留空删除）；validator 的 fetch_time 允许留空；updater 不再同步已停用的旧任务 AGEAnimeUpdater（函数保留）。 |
| v0.6.0 | 找回每日定时自动追更：新增每部番 enabled 启停（停用整部跳过，不查站不下载）；顶层时间字段（HH:MM，留空关闭，v0.6.1 起统一由 fetch_time 承担），保存配置后自动创建/更新/删除计划任务 AniVaultAutoRun（scripts/auto-run.cmd 用 Electron 的 ELECTRON_RUN_AS_NODE=1 跑 updater，仅登录时运行，password 模式不弹 IDM）；面板新增启停开关、定时输入与任务状态展示；旧任务 AGEAnimeUpdater 不动。 |
| v0.5.0 | 三件体验升级：待下载清单改为“剩余待下载”（非 dry-run 只保留失败/未完成集，成功与已存在集剔除，全部成功写空清单，dry-run 保留全部计划）；每集下载状态实时展示（updater 输出 EP_STATUS 结构化行，main 解析后经 run:status / run:episode 暴露，面板显示 下载中/完成/失败）；番剧卡片折叠（已有卡片默认收起、新增卡片默认展开，箭头旋转）。 |
| v0.4.1 | 修复部分成功不推进进度：downloaded_end 更新为原 end 与本次成功/已存在集最高编号的较大值（只前进不倒退），失败集由 auto_repair 自动补；新增 defaults.attempt_timeout_min（默认 20、范围 5-60），单次引擎等待上限由固定 45 分钟改为可配置，只作用于等待文件稳定阶段。 |
| v0.4.0 | 下载引擎可切换（defaults.download_engine：aria2/idm/ffmpeg，默认 aria2，面板下拉）：MP4 所选引擎优先、失败按 aria2→ffmpeg→idm 兜底；M3U8 只用 ffmpeg/aria2；IDM 仅面板交互模式生效，恢复 ensureIdmMinimized 与 auto_close_idm（仅 IDM 成功时）。fetch 加固：请求头 Connection: close、站点地址 AGE_BASE 可覆盖。面板日志区只显示 PROGRESS（BLOCKED 照常写文件）。 |
| v0.3.1 | 修复三个体验问题：fetch 失败自动重试 1 次（间隔 3 秒，超时不重试）后仍失败才记 BLOCKED；查询全部失败且本运行 0 行时保留上次待下载清单；面板日志区合并显示 BLOCKED 末尾。新增 downloaded_start 自动规则（文件夹 ≥1 集→1、0 集→0，数不出时不变）。 |
| v0.3.0 | 下载引擎切换为 aria2（MP4/M3U8 默认 aria2，失败回退 ffmpeg，IDM 退出主流程但函数保留）；defaults 新增 max_parallel（默认 1、范围 1-10、推荐 3），缺失集按并发池下载；fetch 30 秒超时，首页/单番网络失败记 BLOCKED 后继续，不再中断整个运行；主循环抽出可测函数；面板新增 max_parallel 输入并修复日志滚动（上滚不拉回）。 |
| v0.2.0 | 番仓 Demo：新增运行控制——面板触发 dry-run/立即下载、停止子进程、单实例锁（local/run.lock，僵尸锁自动清理）、实时日志、待下载清单表格、打开下载目录；子进程以 ELECTRON_RUN_AS_NODE=1 复用 Electron 运行时；不新增 npm 依赖。 |
| v0.1.0 | 番仓 Demo 首版：Electron 配置面板，可读/改/存 content.json，保存前逐字段校验并原子写盘；保留原脚本与测试为基线；站点选择、多站、下载操作与安装包留待后续。 |
| v1.0 | 首次基线，建立 git 仓库；实现西瓜线路取直链 + IDM 自动下载；按老格式文件夹名（`前缀_片名起-止`）判断进度并改名；每天 18 点计划任务。 |
| v1.1 | 改为 content 驱动，新增 `downloaded_start/downloaded_end/site_latest/folder_name/file_name`；`downloaded_end` 成为做差下载的权威；支持自定义文件夹/文件名模板；dry-run 不再建文件夹、不改名。 |
| v1.2 | 修复自定义 `file_name` 模板的文件识别，模板命名和老格式命名都认，`_L2`/`.failed` 残留不计；顺带修复手动编辑 content.json 缺逗号导致的解析失败。 |
| v1.3 | 新增全局 `defaults`，`max_download`（每次最多下载 N 集）、`auto_repair`（缺失自动补下）、`auto_close_idm`（空闲 60 秒自动关闭 IDM）；IDM 最小化启动；计划任务支持账号+密码模式（`AGE_TASK_USER/AGE_TASK_PASS` + `run_mode`）。 |
| v1.3.1 | 验证密码模式接线，错误密码实测不挂起、不破坏现有任务；补充文档记录。 |
| v1.3.2 | 计划任务正式切换为 Interactive/Background（登录/非登录都运行）；`task_state.json` 更新为 `run_mode: password`。 |
| v1.3.3 | 目录迁移适配，文档命令路径改为新目录；新增保护，密码模式下重装任务若未带 `AGE_TASK_USER/AGE_TASK_PASS` 会拒绝执行，避免任务被悄悄降级为仅登录运行；同步修正 VERSION 文件版本号。 |
| v1.3.4 | 修复下载流程误用未定义站点信息的问题，改为始终使用当前动漫条目的 `site_id`；加入可注入依赖的回归测试，避免取址和线路调用再次串错。 |
| v1.4.0 | 后台计划任务的 MP4 与 M3U8 下载统一改用 ffmpeg，交互模式保留 MP4 使用 IDM；新增 ffmpeg 失败后立即换线路和 `.part.mp4` 原子文件；日志改用上海时区并修正历史时间；本地报告、GitHub 教程和待下载清单集中到被 Git 忽略的 `local/`。 |
