# 设计进度

## 2026-09-01 现状复核

- 本机部署脚本与配置均存在，`enabled.flag=yes`；Lua部署文件SHA-256与仓库源码一致。
- 自动测试与停用态Lua加载已有记录；真实长视频双90%改名的端到端结果仍待记录，不能把“已安装”写成“已完成实机验收”。
- 项目版本随M3U8下载链路校正更新为0.10.2；本文件以下旧记录按发生时间保留，不回写历史。

## 2026-08-29 开工回执

- 目标：只产出 mpv 观看计时与文件夹前缀自动更新的可实施设计和小白说明。
- 顺序：核验本机与官方能力，再比较三条路线，最后写状态机、安装边界和验收。
- 最大风险：mpv 关闭事件发生时文件夹仍被占用，或播放列表中的旧路径因提前改名失效。
- 判定优先级：不误改文件夹，其次准确计时，再考虑自动化和实现规模。
- 文件边界：只写本目录下 DESIGN.md、README.md、PROGRESS.md、BLOCKED.md。
- 基线：分支、HEAD、mpv 版本匹配；pnpm test 退出码 0。

## 2026-08-29 证据核验完成

- 本机 mpv 列出 `path`、`duration`、`pause`、`core-idle`、`paused-for-cache`、`seeking` 属性。
- 本机 uosc 已使用 `end-file`、`event.reason == 'eof'` 与 `utils.subprocess_detached`。
- 官方手册确认 `mp.get_time()`、周期计时器、文件信息、关闭事件和 detached 子进程可用。
- 番仓已导出 `parseFolderName()`、`safeRenameFolder()` 与 `getEpisodeFileMatcher()`，推荐复用。
- 方案选择：Lua 负责计时与持久化，正常关闭时交给独立 Node 助手等待播放器退出后改名。

## 2026-08-29 文档产出完成，进入验收

- `DESIGN.md` 已覆盖三路线比较、唯一推荐架构、状态机、边界、数据流、测试矩阵和安装回滚边界。
- `README.md` 已用小白语言说明未来安装、验证、停用、卸载和不会改名的情况。
- 未来脚本、配置样例和专项测试均规划归档在 `integrations/mpv-watched-prefix/`，未实施、未安装。
- 最终状态以任务书指定命令的新鲜输出为准；本节之后不再修改文件。

## 2026-08-29 双90%与folder_name联动开工回执

- 目标：把观看达标改成有效真实时长和最大播放位置同时达到90%，并让番仓与mpv共用folder_name规则。
- 顺序：先修订判定状态机，再定义`{watched}`模板契约、并发锁和迁移，最后更新小白说明与验收。
- 最大风险：番仓和mpv各自理解模板或并发改名，导致目录找错、覆盖或丢失自定义文字。
- 安全策略：模板必须唯一匹配；没有`{watched}`、匹配0个/多个、目标存在或锁冲突都不改名。
- 简化选择：保留字符串模板，不新增folder_name对象或watched_ep字段；普通文字继续承载自定义前后缀。
- 基线：分支与HEAD匹配；`pnpm test`退出0，Node 9/9、UI 13/13，跳过0。
- 文件边界：仍只修改本目录四份Markdown，不实现、不安装、不修改配置。

## 2026-08-29 双90%与模板联动设计完成

- 状态机改为同时持久化`eligible_seconds`和`max_time_pos`，仅两者都达到视频时长90%才生成合格事件。
- 四个20分钟数据例已写明；2倍速完整观看仍不达标，重复末段仍可能达标，均作为预期行为或已知限制。
- `folder_name`保留字符串形式，计划新增单个`{watched}`作为明确启用开关；不新增`watched_ep`字段。
- 番仓和mpv助手规划共用同一模板生成/匹配入口及`folder-rename.lock`，0个或多个匹配一律不改。
- 已补最小未来改动面、旧目录dry-run迁移、失败恢复、测试矩阵和定性资源分析。

## 2026-08-29 小白说明更新完成，进入验收

- `README.md` 已说明双90%的两道门槛，并给出1倍速、2倍速、seek和重复末段四个20分钟示例。
- 已说明用单个`{watched}`主动启用；缺少或重复该占位符时不进行观看改名。
- 已说明番仓只更新end、mpv只更新watched，自定义文字保持不变，以及旧目录必须先dry-run预览。
- 已说明日常只做固定状态采样、非递归按需目录扫描；具体CPU百分比因未实测而不承诺。
- `BLOCKED.md`保持“无”；下面只做覆盖审计和任务书指定验收，不再改动文档。

## 2026-09-01 内置可选模块形态更新

- 用户入口改为番仓面板中的固定“mpv观看同步”功能卡片，不建设通用插件框架或第三方插件市场。
- 控制拆成三层：是否安装、全局启停、单部动漫是否含`{watched}`；安装完成后默认停用。
- 状态明确为未安装、已停用、已启用、需要更新、外部改动/路径异常，并定义各状态允许的操作。
- 计划新增Node标准库管理器并沿用现有Electron IPC链；不再把独立PowerShell脚本作为主要入口。
- 更新和卸载必须核对部署哈希；外部改动时拒绝覆盖或删除，半安装必须恢复原状态。
- `README.md`已同步未来面板安装、启停、检查状态和卸载流程；当前仍未实现或安装。

## 2026-09-01 实现前四项契约补齐

- 启停改用项目本地`enabled.flag`作为唯一来源，部署Lua与配置保持不变，解决正常开关被误判为外部改动的问题。
- 陈旧锁规则固定为“PID确认死亡且锁龄达到10分钟”后原子改名保留证据并只重试一次；不确定时仍不改名。
- 跨会话身份改为动漫配置键、目录内相对视频路径、size和mtime；受控父目录改名后可唯一重绑定，歧义时失效。
- 管理器5个Promise接口、5个IPC名称、统一返回结构、两个部署目标及install-state v1格式已固定。
- 安装/更新/卸载使用事务日志、同目录临时文件和备份逆序恢复；下次管理器调用先恢复未完成事务。
- 事件ID、committed_seq和“目录已到目标但提交未落盘”的幂等恢复规则已明确。

## 2026-09-01 实现开发开工回执

- 目标：按现有DESIGN实现番仓内置、可选安装和启停的mpv观看同步，不在本轮写入真实mpv目录。
- 顺序：模板与锁、观看助手与持久化、安装管理器、Lua与Electron面板、版本文档和整体验收。
- 最大风险：目录模板歧义、并发改名和安装中断可能误改用户目录或覆盖现有mpv脚本。
- 安全选择：所有不唯一、目标冲突、锁身份不明和部署哈希异常都拒绝写入并保留证据。
- 分支已从master切回用户准备的`codex/auto-update-watched-prefix`；HEAD仍为`2d6aff46027ca3849f2b23c18de865e8fbd15c75`。
- 基线`pnpm test`退出0：Node test runner 9/9、UI检查13/13，均0失败、0跳过。
- 现有`AGENTS.md`、根`BLOCKED.md`、根`PROGRESS.md`和`content.json`现场改动只保留，不回滚。

## 2026-09-01 模板契约与共享锁完成

- 新增专项红灯测试后实现：`folder_name`允许至多一个`{watched}`，`file_name`继续拒绝该占位符。
- `applyTemplate()`、`matchFolderTemplate()`、`resolveFolderName()`和`findFolder()`现在使用同一模板语义；下载更新`end`时保留已捕获的`watched`。
- 新增`src/folder-rename-lock.js`：原子创建、所有权令牌释放、活锁/未知锁拒绝、死锁满10分钟保留stale证据后只重试一次。
- Node专项验证：模板5项和锁4项全部通过，0失败、0跳过。
- Node标准库不能跨平台可靠核对任意进程的启动时间；实现采取更保守规则：只要PID仍存活就视为busy，不会因PID复用而抢锁。

## 2026-09-01 观看助手、管理器和面板实现完成

- Node助手已实现双90%合并、session+seq幂等、最后达标集优先、模板唯一匹配、目标冲突拒绝和共享锁内改名。
- mpv Lua已实现停用早退、每秒采样、四个状态阻断、2秒睡眠封顶、10秒JSONL落盘，以及仅在正常shutdown启动detached助手。
- 管理器已实现未安装/停用/启用/需更新/外部改动/路径异常状态，以及安装、更新、启停、卸载和事务恢复；所有安装测试只使用临时mpv目录。
- Electron已接入五个固定IPC和“mpv观看同步”卡片；安装后默认停用，单部动漫仍需一个`{watched}`加入。
- 番仓目录改名改用Node原生`renameSync`并接入同一共享锁，避免把目录名拼进PowerShell命令。
- 21项专项Node测试全部通过；本机mpv 0.40以停用态加载Lua退出0，证明脚本可解析。第一次冒烟命令因PowerShell参数拼接错误未加载脚本，已用简化命令复跑成功。
- 版本已同步到0.10.0；真实mpv安装与长视频90%联调仍未执行。

## 2026-09-01 崩溃恢复补齐与最终验收准备

- 新增红灯测试证明旧实现不会读取崩溃会话；助手已改为扫描state目录全部session JSONL，按事件时间合并并用各session的committed_seq去重。
- 崩溃会话45秒加当前会话45秒、最大位置90秒的恢复测试已转绿，随后全量Node test runner为31/31、0失败、0跳过。
- `pnpm run smoke`输出`SMOKE_OK`，`pnpm run selftest`输出`SELFTEST_OK`；Chromium缓存目录曾报拒绝访问，但两项退出码均为0。
- `pnpm test`退出0；UI静态检查15/15、0失败、0跳过。
- 本机真实部署目标`F:\Backend\mpv.lite\portable_config\scripts\anivault-watched.lua`仍不存在，确认本轮没有安装。
- 事务日志现会在脚本、配置、安装记录和启停标志每一步后更新phase；专项管理器5/5和最终`pnpm test`再次退出0。

## 2026-09-01 AGE_DLOAD 面板配置

- 全局设置新增下载目录输入框，配置写入 `content.json` 的 `download_dir`；环境变量 `AGE_DLOAD` 继续优先，并在面板显示当前生效路径和来源。
- updater、Electron 主进程和配置校验已统一该优先级；新增路径校验、配置优先级测试和 UI 静态检查。
- `pnpm test` 退出0（33个Node子测试、36项配置检查、16项UI检查均通过）。

## 2026-09-01 下载残留与重复下载修复

- 根因确认不是目录路径或Git提交时机：ffmpeg完成后将`.part.mp4`改为正式文件名时遭遇Windows临时占用并返回`EPERM`，随后程序依次下载其余线路，且下次运行不识别临时文件。
- 改名统一对`EPERM/EBUSY`进行最多10秒有限重试；下次运行会从同一集各线路的临时文件中选择时长完整者恢复，不删除其他残留。
- 有上一集可参照时，新文件和恢复候选的时长必须达到上一集80%，因此实测7分01秒候选不会替代约24分钟正片。
- 版本同步为0.10.1；新增5项专项回归测试，全量`pnpm test`退出0（38个Node子测试、36项配置检查、16项UI检查均通过）。
